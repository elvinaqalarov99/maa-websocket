version: "3"

services:
  ws:
    restart: on-failure
    build:
      context: .
      dockerfile: Dockerfile.dev
    volumes:
      - ./src:/app/src
    ports:
      - "8001:8080"
    environment:
      - DATABASE_URL=${DATABASE_URL}
